import { GradientEdge } from "./gradient-edge"
import { AnimatedFlowEdge } from "./animated-flow-edge"

export const edgeTypes = {
  gradient: GradientEdge,
  animatedFlow: AnimatedFlowEdge,
}

export { GradientEdge, AnimatedFlowEdge }
